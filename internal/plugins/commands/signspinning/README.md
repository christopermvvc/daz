# sign_spinning command

Spin signs on the street corner for cash (economy credit).

Config (optional)
- `cooldown_hours` (default: 36)

Notes
- Uses the economy API to credit earnings.
- Stats are tracked in `daz_sign_spinning_stats`.
